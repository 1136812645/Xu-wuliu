{
  "name": "waybill-settlement-platform",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/api",
    "apps/web"
  ],
  "scripts": {
    "dev": "npm run dev --workspace @waybill/api",
    "dev:api": "npm run dev --workspace @waybill/api",
    "dev:web": "npm run dev --workspace @waybill/web",
    "build": "npm run build --workspaces",
    "check": "npm run check --workspaces",
    "test": "npm run test --workspace @waybill/api"
  },
  "engines": {
    "node": ">=22"
  }
}
