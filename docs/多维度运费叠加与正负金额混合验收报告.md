# 多维度运费叠加与正负金额混合验收报告（2026-07-06）

## 验收项

1. 每一项费用独立展示明细，可溯源；无费用漏算。
2. 正负金额加减逻辑无精度丢失（保留两位小数）。
3. 总金额负数场景可正常保存运单，结算页面可展示负金额应付，并有明细数据。

## 用例与结果

### 用例 A：费用明细独立展示与可溯源

请求：`POST /api/waybills/quote`

输入摘要：

- mileageKm=100
- extraLoadingFee=60
- subsidy=100
- deduction=20

结果：

- feeCount=5
- feeTypes=LINE_HAUL, LOADING, INSURANCE, SUBSIDY, DEDUCTION
- formulaLineHaul=`100km x 8.2`
- formulaLoading=`180 + 60`
- total=1149.84

结论：通过（费用项完整、公式可追溯、无漏算）。

### 用例 B：正负金额混合与精度

请求：`POST /api/waybills/quote`

输入摘要：

- mileageKm=123.45
- extraLoadingFee=12.34
- subsidy=-45.67
- deduction=89.01

结果：

- lineHaul=1012.29
- loading=192.34
- insurance=12.15
- subsidy=-45.67
- deduction=-89.01
- total=1082.1
- sumByItems=1082.1
- precisionOk=true

结论：通过（正负金额加减正确，汇总与明细和一致，保留两位小数）。

### 用例 C：负总额保存与展示

步骤：

1. 通过规则接口将首段运价调低（用于构造负总额）。
2. 创建运单：mileageKm=10、subsidy=-200、deduction=50。

结果：

- waybillNo=WB26618453
- totalAmount=-240
- isNegative=true
- feeItems=5
- 明细：
  - LINE_HAUL=10
  - LOADING=0
  - INSURANCE=0
  - SUBSIDY=-200
  - DEDUCTION=-50

结论：通过（负总额可保存且明细完整）。

## 结算页面展示证据

前端列表金额渲染统一调用 `money(item.totalAmount)`，对负数金额同样生效：

- `apps/web/src/App.tsx` 运单列表金额列使用 `money(item.totalAmount)`。

说明：`Intl.NumberFormat` 金额格式对负值会输出负金额货币格式，可在结算/运单列表直接展示负金额应付。

## 最终结论

- 验收标准 1：通过
- 验收标准 2：通过
- 验收标准 3：通过

---

## 补充验收（2026-07-08）

### 补充目的

在原报告结论基础上，补充本轮代码完善后的可回归证据，重点覆盖：

1. 金额精度防漂移实现是否稳定。
2. 负总额运单是否可保存并在结算页可视化展示。
3. 明细可溯源能力是否在前后端一致落地。

### 本轮代码完善摘要

后端：

- 费用汇总改为“按分整型累计后再还原两位小数”，避免浮点累计误差。
- 拆分分摊使用分级分配，保证拆分前后金额总和一致。

前端：

- 结算页新增“结算应付明细”表，展示 运单号、状态、应付金额、费用明细。
- 负金额应付显式标注“负金额应付”。
- 明细展示包含 fee label + amount + formula，满足可追溯要求。

### 自动化测试补充结果

后端测试执行：`npm run test --workspace @waybill/api`

- 结果：13/13 通过。
- 新增并通过的关键测试：
  - mixed signs + cent-level precision（正负混合且无浮点漂移）
  - negative total waybill persistence（负总额可落单且明细保留）

### 接口实测补充证据

#### 补充用例 D：正负混合费用与精度（新基线）

请求：`POST /api/waybills/quote`

结果：

- mixedTotal=1175.81
- feeTypes=`LINE_HAUL:984,LOADING:230,INSURANCE:11.81,SUBSIDY:30,DEDUCTION:-80`

结论：通过（加减逻辑正确，明细与总额一致）。

#### 补充用例 E：负总额运单保存

请求：`POST /api/waybills`

输入关键参数：

- mileageKm=0
- extraLoadingFee=0
- subsidy=0
- deduction=180

结果：

- waybillNo=WB90069360
- totalAmount=-60
- feeCount=5

结论：通过（负总额可正常保存）。

### 结算页面展示补充证据

页面检查结果（结算规则页）：

- hasNegativeTag=1（存在“负金额应付”标识）
- hasWaybillNo=1（存在 WB90069360 行）
- amountCell=1（存在 `-¥60.00`）
- detailContainsDeduction=3（页面可见扣款明细项）

页面样例行：

- `WB90069360 已分配 -¥60.00 (负金额应付)`
- 明细包含：
  - 干线运费 / Line haul: `¥0.00 (0km x 6.9)`
  - 装卸费 / Loading: `¥120.00 (120 + 0)`
  - 保险费 / Insurance: `¥0.00 (0 x 0.01)`
  - 补贴 / Subsidy: `¥0.00 (0)`
  - 扣款 / Deduction: `-¥180.00 (180 x -1)`

结论：通过（负金额应付与明细均可在结算页面直接验收）。

## 补充最终结论

- 验收标准 1（明细独立可溯源、无漏算）：通过。
- 验收标准 2（正负金额精度稳定，两位小数）：通过。
- 验收标准 3（负总额可保存并在结算页展示明细）：通过。
