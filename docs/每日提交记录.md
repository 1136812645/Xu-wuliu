# 每日提交记录

> 说明：本文件用于过程管控验收，记录每天提交主题、粒度、验证结果。

## 2026-07-06

1. feat(api): add pricing rule management endpoints
- 改动：新增 `GET /api/pricing-rules`、`POST /api/pricing-rules`、`POST /api/pricing-rules/reload`
- 目的：支持结算规则配置化与快速微调
- 验证：接口手工调用返回 200/201

2. feat(api): add structured log retention
- 改动：新增 `apps/api/src/logger.ts`，接入请求与核心业务日志落盘
- 目的：支持 bug 排查日志留存
- 验证：生成 `apps/api/logs/api-YYYY-MM-DD.log`

3. docs(process): add commit and troubleshooting process docs
- 改动：新增提交规范与每日提交记录模板，更新故障排查文档
- 目的：补齐过程管控交付物
- 验证：文档可用于验收答辩

## 2026-07-07

1. feat(perf): add chunked waybill import API and 10k stream benchmark
- 改动：新增批量导入 chunk 接口、流式模板与压测脚本
- 目的：支撑 10k+ 运单导入验收
- 验证：批量导入接口与脚本可执行

2. fix(logging): avoid duplicate success logs in idempotent sign/pod
- 改动：修复重复签收/重复回单场景下的成功日志污染
- 目的：保证问题复盘日志准确
- 验证：重复幂等调用不再输出成功业务日志

3. docs(design): complete flow diagrams and code mapping for acceptance
- 改动：补齐设计图、ER 图、技术方案与代码映射文档
- 目的：满足验收材料要求
- 验证：设计文档与验收报告可追溯到代码实现

说明：

- 当日实际历史中存在部分不规范备注（如 `fix` / `修改` / `xu-wuliu`），已在 `docs/git-commit-granularity-validation-report.md` 中单独标记为整改项。
